using System;
using Autofac;

namespace MartDayOff.iOS
{
    public class ContainerStartup_iOS :ContainerStartup
    {
        public ContainerStartup_iOS()
        {
        }

        protected override void Register(ContainerBuilder builder)
        {
            base.Register(builder);
        }
    }
}
